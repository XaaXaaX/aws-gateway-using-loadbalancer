{
  "name": "aws-gateway-using-loadbalancer",
  "version": "0.1.0",
  "bin": {
    "aws-gateway-using-loadbalancer": "bin/aws-gateway-using-loadbalancer.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk:app": "cdk --app 'ts-node --prefer-ts-exts ./bin/aws-gateway-using-loadbalancer.ts'",
    "cdk:app:dev": "npm run cdk:app -- --profile serverless -c env=dev --all --require-approval never"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.137",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.12.8",
    "aws-cdk": "2.135.0",
    "esbuild": "^0.20.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "~5.4.3"
  },
  "dependencies": {
    "aws-cdk-lib": "2.135.0",
    "constructs": "^10.0.0",
    "source-map-support": "^0.5.21"
  }
}
